SELECT top 10 * FROM PROFILE p where p.url like '%#%';
select top 10 * from profile  p where p.url  like  'http://freebdsmsexvideos.net/2016/01/08/she-watches-as-her%';
select top 10 url, max(level) level, status from profile where status = ? group by url

truncate table profile 
SELECT STATUS , count(1) FROM PROFILE group by STATUS  order by STATUS ;
SELECT level , count(1) FROM PROFILE group by level ;
SELECT url , level,  count(1) FROM PROFILE group by url, level  having count(1)>1;